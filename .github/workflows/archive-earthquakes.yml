name: Build notable earthquake archive

on:
  schedule:
    - cron: "23 */6 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  archive:
    runs-on: ubuntu-latest

    env:
      BRAMBLEY_LAT: ${{ secrets.BRAMBLEY_LAT }}
      BRAMBLEY_LON: ${{ secrets.BRAMBLEY_LON }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Build notable archive
        run: python archive_earthquakes.py

      - name: Add public earthquake coordinates and depth
        run: python enrich_archive_metadata.py

      - name: Build recent M3 regional capture feed
        run: python recent_regional_captures.py

      - name: Commit archive changes
        run: |
          git config user.name "Brambley Observatory"
          git config user.email "actions@users.noreply.github.com"
          git add docs/archive docs/archive.json docs/archive-processed.json
          git add docs/recent-captures docs/recent-captures.json docs/recent-captures-processed.json
          if git diff --cached --quiet; then
            echo "No archive changes to publish."
          else
            git commit -m "Update earthquake observations"
            git push
          fi
